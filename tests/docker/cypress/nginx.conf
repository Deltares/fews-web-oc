worker_processes 4;

events { worker_connections 1024; }

http {
    server {
        listen 80;
        root  /app;
        include /etc/nginx/mime.types;
        server_tokens off;
        location / {
            try_files $uri /index.html =404;
        }
    }
}
